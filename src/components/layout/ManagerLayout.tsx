import { Layout } from './Layout';

/**
 * Manager Layout Component
 * Wraps manager pages with the standard layout (navbar + sidebar)
 */
export const ManagerLayout = Layout;
export default ManagerLayout;
